## 1. Фабрика провайдеров памяти

- [x] 1.1 Создать `src/prokop/memory/factory.py`: реестр доступных провайдеров и `build_provider(config, home)`
- [x] 1.2 Неизвестное имя — предупреждение и отсутствие внешнего провайдера
- [x] 1.3 `build_manager(config, home)` — менеджер с подключённым внешним провайдером
- [x] 1.4 `config.py`: `memory.options` (аддитивно)

## 2. Файловый провайдер памяти

- [x] 2.1 Создать `src/prokop/memory/file_provider.py` с хранилищем JSONL в профиле
- [x] 2.2 Запись фактов и дайджестов ходов; чтение с пропуском битых строк
- [x] 2.3 `prefetch` по пересечению токенов (регистр, кириллица), лимит записей, пустой запрос — свежие факты
- [x] 2.4 `sync_turn` — запись дайджеста хода
- [x] 2.5 Инструменты `memory_save`, `memory_search`, `memory_forget` (схемы + обработка)
- [x] 2.6 Ограничение числа записей и атомарная компактизация
- [x] 2.7 `backup_paths()` — путь хранилища
- [x] 2.8 Устойчивость: отсутствующее хранилище и ошибки чтения не ломают память

## 3. Адаптер Telegram

- [x] 3.1 Создать `src/prokop/gateway/telegram.py` на `httpx` (клиент инжектируется)
- [x] 3.2 `connect` (проверка учётных данных) и `disconnect`
- [x] 3.3 `send_text` с разбиением по лимиту и контекстом ответа
- [x] 3.4 `set_typing`, `chat_info`, `send_media`
- [x] 3.5 Отображение ответов и сбоев в категории ошибок контракта; без исключений наружу
- [x] 3.6 `get_updates(offset, timeout)` — длинный опрос
- [x] 3.7 `normalize(update)` → нормализованное событие (текст, медиа, автор, источник, тред, контекст ответа)

## 4. CLI

- [x] 4.1 `prokop memory list`
- [x] 4.2 `prokop memory search <запрос>`
- [x] 4.3 `prokop memory forget <ключ>`
- [x] 4.4 Работа без ключа модели; понятное сообщение при не-файловом провайдере

## 5. Тесты

- [x] 5.1 Фабрика: отсутствие настройки, `builtin`, `file`, неизвестное имя
- [x] 5.2 Файловая память: сохранение/чтение, переживание перезапуска, дайджест хода
- [x] 5.3 Релевантность: совпадение, отсутствие совпадения, кириллица, лимит
- [x] 5.4 Инструменты памяти: сохранение, поиск, удаление, отсутствие аргумента
- [x] 5.5 Ограничение объёма и компактизация; битые строки; отсутствующее хранилище
- [x] 5.6 Telegram: подключение (успех/отказ), отправка, разбиение длинного текста, контекст ответа
- [x] 5.7 Telegram: категории ошибок (авторизация, лимит, сервер, сеть, неверные данные)
- [x] 5.8 Telegram: нормализация текста, медиа, треда, ответа, обновления без сообщения
- [x] 5.9 Telegram: длинный опрос передаёт позицию и время ожидания
- [x] 5.10 CLI `memory list|search|forget` без ключа модели
- [x] 5.11 Тест-страж: в новых исходниках нет личных абсолютных путей

## 6. Документация и проверка

- [x] 6.1 README: раздел о памяти и адаптере платформы
- [x] 6.2 `docs/GUIDE.md`: настройка провайдера памяти, подкоманда `memory`, адаптер Telegram
- [x] 6.3 `docs/PARITY.md`: отметить закрытие гэпа «реализации контрактов»
- [x] 6.4 CHANGELOG: запись о добавлении
- [x] 6.5 Прогон `pytest src/tests -q` (все зелёные) и `openspec validate --all`
