## 1. Чистая логика TUI

- [x] 1.1 Создать пакет `src/prokop/tui/` (`__init__.py`, `state.py`, `app.py`)
- [x] 1.2 `state.py`: состояние сессии (провайдер, модель, идентификатор сессии, число ходов, история)
- [x] 1.3 `state.py`: статусная строка
- [x] 1.4 `state.py`: разбор слэш-команд (`/help`, `/status`, `/model`, `/provider`, `/new`, `/sessions`, `/clear`, `/quit`) как чистая функция
- [x] 1.5 `state.py`: неизвестная команда — сообщение об ошибке, интерфейс не завершается
- [x] 1.6 `state.py`: строка без `/` трактуется как запрос к модели
- [x] 1.7 `state.py`: модуль не импортирует `prompt_toolkit` и не зависит от терминала

## 2. Интерактивный цикл

- [x] 2.1 `app.py`: ленивый импорт `prompt_toolkit`; при недоступности — сообщение с командой установки и предложение линейного режима
- [x] 2.2 `app.py`: проверка терминала; без TTY — сообщение и код `1`
- [x] 2.3 `app.py`: разрешение провайдера и ключа через существующий помощник CLI
- [x] 2.4 `app.py`: цикл ввода с историей и статусной строкой
- [x] 2.5 `app.py`: потоковый вывод ответа через колбэки ядра
- [x] 2.6 `app.py`: запись сообщений пользователя и ассистента в хранилище сессий (источник `tui`)
- [x] 2.7 `app.py`: `/new` создаёт новую сессию, `/clear` чистит историю процесса
- [x] 2.8 `app.py`: ошибка хода печатается и не завершает интерфейс
- [x] 2.9 `commands.py`: публичный помощник одного хода (переиспользование в TUI)

## 3. CLI и упаковка

- [x] 3.1 `pyproject.toml`: extra `tui` с `prompt_toolkit`
- [x] 3.2 `main.py`: подкоманда `tui` с флагами `--model` и `--provider`
- [x] 3.3 `commands.py`: реализация `tui_command` с кодами возврата

## 4. Тесты

- [x] 4.1 Разбор слэш-команд: все команды, неизвестная, обычный текст
- [x] 4.2 Смена модели и провайдера, отказ на неизвестного провайдера
- [x] 4.3 `/new` и `/clear` меняют состояние предсказуемо
- [x] 4.4 Статусная строка содержит провайдера, модель и счётчик ходов
- [x] 4.5 Деградация без зависимости (импорт подменён) и без терминала
- [x] 4.6 Запись хода в хранилище сессий на фейковом цикле хода
- [x] 4.7 Ошибка хода не завершает интерфейс (фейковый цикл возвращает ошибку)
- [x] 4.8 Тест-страж: `state.py` не импортирует `prompt_toolkit`

## 5. Документация и проверка

- [x] 5.1 README: раздел о TUI и extra `tui`
- [x] 5.2 `docs/GUIDE.md`: команда `tui`, слэш-команды, деградация
- [x] 5.3 `docs/PARITY.md`: отметить закрытие гэпа TUI
- [x] 5.4 CHANGELOG: запись о добавлении
- [x] 5.5 Прогон `pytest src/tests -q` (все зелёные) и `openspec validate --all`
