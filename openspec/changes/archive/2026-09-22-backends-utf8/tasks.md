## 1. Реализация

- [x] 1.1 В `src/prokop/backends/local.py` перевести `Popen` на явную UTF-8-декодировку (`encoding="utf-8"`, `errors="replace"`)
- [x] 1.2 Добавить в сборку окружения UTF-8-переменные (`PYTHONUTF8=1`, `PYTHONIOENCODING=utf-8`) с сохранением приоритета: `os.environ` → аргумент `env` → снимок сессии
- [x] 1.3 Не перезаписывать UTF-8-переменные, заданные вызывающим кодом

## 2. Тесты

- [x] 2.1 Тест: дочерний процесс печатает кириллицу — точное совпадение в `output`
- [x] 2.2 Тест: результат не зависит от `PYTHONIOENCODING` в окружении вызывающей стороны
- [x] 2.3 Тест: нераскодируемые байты не роняют команду (символ замены)
- [x] 2.4 Тест: явное переопределение `PYTHONIOENCODING` не перезаписывается
- [x] 2.5 Тест: переменная снимка сессии сохраняет приоритет

## 3. Документация и проверка

- [x] 3.1 `docs/PLATFORM_NOTES.md`: убрать кодировку вывода из таблицы платформенных различий, зафиксировать компромисс для OEM-вывода нативных утилит
- [x] 3.2 CHANGELOG: запись об исправлении
- [x] 3.3 Прогон `pytest src/tests -q` (все тесты зелёные, в том числе при заданной `PYTHONIOENCODING`) и `openspec validate --all`
