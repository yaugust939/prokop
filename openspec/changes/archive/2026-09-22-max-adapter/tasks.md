## 1. Адаптер MAX

- [x] 1.1 Создать `src/prokop/gateway/max.py` на `httpx` (клиент инжектируется), база `platform-api2.max.ru`, заголовок `Authorization`
- [x] 1.2 `connect` (`GET /me`) и `disconnect`
- [x] 1.3 `send_text`: `POST /messages`, разбиение по лимиту 4000, контекст ответа через `link`
- [x] 1.4 Адресация: `chat_id` по умолчанию, `user:<id>` → `user_id`
- [x] 1.5 `set_typing`: `POST /chats/{chatId}/actions` только для групповых чатов, best-effort
- [x] 1.6 `chat_info`: `GET /chats/{chatId}` с отображением вида чата и запасным значением при сбое
- [x] 1.7 `send_media`: тип по расширению, `POST /uploads` → загрузка по `url` → токен → `POST /messages` с вложением
- [x] 1.8 `get_updates`: `GET /updates` с `limit`/`timeout`/`marker`, хранение указателя в адаптере
- [x] 1.9 `normalize`: `message_created` и `message_callback` → событие гейтвея; прочие типы игнорируются
- [x] 1.10 Категории ошибок по документированным кодам (401/429/5xx/400/404/405, сеть); `send_text` без исключений

## 2. Тесты

- [x] 2.1 Подключение: успех, отказ авторизации, сетевой сбой, обязательный токен
- [x] 2.2 Отправка текста: успех, идентификатор как строка, разбиение длинного текста, контекст ответа только в первой части, пустой текст
- [x] 2.3 Адресация: чат по умолчанию, `user:<id>` без префикса в запросе
- [x] 2.4 Индикатор набора: групповой чат, диалог (без запроса), сбой не поднимается
- [x] 2.5 Информация о чате: успех и запасное значение при ошибке
- [x] 2.6 Медиа: двухшаговая загрузка, токен из первого или второго ответа, отсутствующий файл, тип по расширению
- [x] 2.7 Опрос: указатель сохраняется и передаётся, ошибка даёт пустой список
- [x] 2.8 Нормализация: сообщение, виды чата, вложения, ответ, нажатие кнопки, пост канала без отправителя, прочие события
- [x] 2.9 Категории ошибок: авторизация, лимит, сервис недоступен, неверные данные, сеть
- [x] 2.10 Тест-страж: в исходнике нет личных абсолютных путей

## 3. Документация и проверка

- [x] 3.1 README: адаптер MAX рядом с Telegram
- [x] 3.2 `docs/GUIDE.md`: пример работы с MAX, отличия от Telegram
- [x] 3.3 `docs/PARITY.md`: отметить второй адаптер
- [x] 3.4 CHANGELOG: запись о добавлении
- [x] 3.5 Прогон `pytest src/tests -q` (все зелёные) и `openspec validate --all`
