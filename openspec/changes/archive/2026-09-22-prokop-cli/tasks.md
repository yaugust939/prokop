## 1. Каркас CLI

- [x] 1.1 Создать пакет `src/prokop/cli/` (`__init__.py`, `main.py`, `commands.py`, `stdio.py`)
- [x] 1.2 Добавить `[project.scripts] prokop = "prokop.cli.main:main"` в `src/pyproject.toml`
- [x] 1.3 Реализовать `stdio.py`: приведение потоков к UTF-8 с no-op на платформах без поддержки
- [x] 1.4 Реализовать разбор аргументов (`argparse`, подкоманды) и `--version`/`--help`; переопределить код ошибки разбора на `1`
- [x] 1.5 Реализовать глобальный флаг `--json` и два форматтера вывода (текст/JSON, `ensure_ascii=False`)
- [x] 1.6 Реализовать фиксированные коды возврата (`0`/`1`/`2`) и единый обработчик ошибок команд

## 2. Локальные команды

- [x] 2.1 `config show` — действующая конфигурация профиля + путь к домашнему каталогу (без записи файлов)
- [x] 2.2 `doctor` — проверка каталога профиля, конфигурации, ключа модели и внешних бинарей; без обращения к модели
- [x] 2.3 `sessions list|show|search` — перечисление, показ по id, полнотекстовый поиск
- [x] 2.4 `skills list` — доступные навыки профиля
- [x] 2.5 `providers list` — провайдеры, обнаруженные реестром
- [x] 2.6 `cron list|add|tick` — перечисление, создание с расписанием, исполнение одного тика

## 3. Сетевые команды

- [x] 3.1 `turn "<запрос>"` — один ход через модель из конфигурации профиля
- [x] 3.2 Понятная ошибка и код `1` при отсутствии ключа модели (без сетевых вызовов)
- [x] 3.3 `chat` — линейный REPL: выход по команде и по концу потока; работа без TTY

## 4. Тесты

- [x] 4.1 `src/tests/test_cli.py`: разбор аргументов, `--version`/`--help`, неизвестная подкоманда → код `1`
- [x] 4.2 Тесты кодов возврата (`0`/`1`/`2`) и неверных аргументов → `1`
- [x] 4.3 Тесты форматов вывода: валидность JSON и кириллица без `\uXXXX`
- [x] 4.4 Тесты локальных команд на временном `PROKOP_HOME` (без сети и ключа)
- [x] 4.5 Тест `chat` без TTY: поданный поток обрабатывается, код `0`
- [x] 4.6 Тест-страж: в исходниках CLI нет личных абсолютных путей

## 5. Документация и проверка

- [x] 5.1 README: раздел «Командная строка» с быстрым стартом
- [x] 5.2 `docs/GUIDE.md`: раздел CLI (команды, форматы, коды возврата)
- [x] 5.3 `docs/PARITY.md`: отметить закрытие гэпа CLI/TUI (CLI — закрыт, TUI — остаётся)
- [x] 5.4 CHANGELOG: запись о добавлении точки входа
- [x] 5.5 Прогон `pytest src/tests -q` (все тесты зелёные) и `openspec validate --all`
