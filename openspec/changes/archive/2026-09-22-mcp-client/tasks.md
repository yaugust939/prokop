## 1. Клиент MCP

- [x] 1.1 Создать пакет `src/prokop/mcp/` (`__init__.py`, `client.py`, `config.py`, `runtime.py`)
- [x] 1.2 `client.py`: запуск процесса сервера списком аргументов, stdio в UTF-8 с заменой
- [x] 1.3 `client.py`: JSON-RPC 2.0 поверх построчных сообщений — запрос, уведомление, сопоставление `id`, разбор ошибки
- [x] 1.4 `client.py`: рукопожатие `initialize` → `notifications/initialized`
- [x] 1.5 `client.py`: `tools/list` с значениями по умолчанию для описания и схемы
- [x] 1.6 `client.py`: `tools/call` с JSON-строкой результата и признаком ошибки
- [x] 1.7 `client.py`: таймаут с завершением процесса и освобождением ресурсов; идемпотентное `close()`
- [x] 1.8 `config.py`: разбор секции `mcp.servers` в описания серверов, выключенные по умолчанию, устойчивость к битым описаниям
- [x] 1.9 `config.py`: поле `mcp` в `Config` (`prokop/config.py`), аддитивно
- [x] 1.10 `runtime.py`: владение клиентами, санитайзинг имён `mcp__<сервер>__<инструмент>` с обратной картой
- [x] 1.11 `runtime.py`: регистрация инструментов в `ToolRegistry` (набор `mcp`), пропуск недоступных серверов с предупреждением
- [x] 1.12 `runtime.py`: асинхронный обработчик вызова, `aclose()` для закрытия всех клиентов
- [x] 1.13 `tools/toolsets.py`: набор `mcp`

## 2. CLI

- [x] 2.1 `prokop mcp list` — настроенные серверы с признаком включённости
- [x] 2.2 `prokop mcp tools <сервер>` — инструменты сервера
- [x] 2.3 `prokop mcp call <сервер> <инструмент> --args '<json>'` — вызов с разбором JSON и кодом `1` на невалидный JSON
- [x] 2.4 Ошибка на неизвестный сервер с кодом `1`

## 3. Тесты

- [x] 3.1 Тестовый MCP-сервер-заглушка на stdlib (файл в `tests/`), отвечающий на `initialize`, `tools/list`, `tools/call`
- [x] 3.2 Тесты клиента: рукопожатие, список инструментов, вызов, кириллица
- [x] 3.3 Тест таймаута: молчащий сервер → ошибка, процесс завершён
- [x] 3.4 Тест разбора конфигурации: отсутствие секции, выключенный по умолчанию, битое описание
- [x] 3.5 Тест санитайзинга имён и обратной карты (вызов уходит под исходным именем)
- [x] 3.6 Тест регистрации в реестре и вызова через диспетчер ядра
- [x] 3.7 Тест устойчивости: недоступный сервер не ломает остальные
- [x] 3.8 Тесты CLI `mcp list|tools|call` на локальном сервере-заглушке
- [x] 3.9 Тест-страж: в исходниках пакета `mcp` нет личных абсолютных путей

## 4. Документация и проверка

- [x] 4.1 README: раздел о подключении внешних MCP-серверов
- [x] 4.2 `docs/GUIDE.md`: секция `mcp.servers` и подкоманда `mcp`
- [x] 4.3 `docs/PARITY.md`: отметить закрытие гэпа «MCP-клиент»
- [x] 4.4 CHANGELOG: запись о добавлении
- [x] 4.5 Прогон `pytest src/tests -q` (все зелёные) и `openspec validate --all`
