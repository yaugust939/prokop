## 1. Политики

- [x] 1.1 Создать пакет `src/prokop/security/` (`__init__.py`, `policy.py`, `audit.py`)
- [x] 1.2 `policy.py`: модель решения (`allow`/`ask`/`deny`) с видом, предметом и причиной
- [x] 1.3 `policy.py`: разбор секции `security` конфигурации (команды deny/allow, пути deny/allow, аудит)
- [x] 1.4 `policy.py`: `check_command()` с фиксированным приоритетом правил поверх `tools/safety`
- [x] 1.5 `policy.py`: `check_path()` с нормализацией пути (разделители, регистр, кириллица)
- [x] 1.6 `policy.py`: ленивая политика из конфигурации профиля и сброс кэша
- [x] 1.7 `config.py`: секция `security` (аддитивно)

## 2. Аудит

- [x] 2.1 `audit.py`: запись решения в JSONL в каталоге логов профиля
- [x] 2.2 `audit.py`: устойчивость к сбою записи (не прерывать операцию)
- [x] 2.3 `audit.py`: чтение хвоста с ограничением числа записей
- [x] 2.4 `audit.py`: выключение журнала по конфигурации

## 3. Интеграция

- [x] 3.1 `tools/dispatcher.py`: решения по командам принимает политика (deny из конфигурации, allow, аудит)
- [x] 3.2 `tools/dispatcher.py`: жёсткий блок-лист остаётся непереопределяемым
- [x] 3.3 `checkpoints/store.py` + `tool.py`: проверка политики путей при создании снимка и восстановлении
- [x] 3.4 Причина запрета попадает в отчёт о восстановлении

## 4. CLI

- [x] 4.1 `prokop security show` — правила и указание границ слоя
- [x] 4.2 `prokop security check "<команда>"` — решение без выполнения
- [x] 4.3 `prokop security audit [--limit N]` — хвост журнала
- [x] 4.4 Подкоманда работает без ключа модели

## 5. Тесты

- [x] 5.1 Приоритет: жёсткий блок-лист сильнее allow; deny сильнее allow
- [x] 5.2 Разрешающий список снимает подтверждение для опасной команды
- [x] 5.3 Отсутствие секции `security` — поведение как раньше
- [x] 5.4 Решения по путям: запрет, разрешение перекрывает запрет, без правил
- [x] 5.5 Нормализация пути: кириллица, смешанные разделители, регистр
- [x] 5.6 Аудит: запись, чтение хвоста, лимит, выключение, сбой записи
- [x] 5.7 Диспетчер: запрет из конфигурации, разрешение, запись в аудит
- [x] 5.8 Снимки: запрещённый путь при восстановлении и при создании
- [x] 5.9 CLI `security show|check|audit` без ключа модели
- [x] 5.10 Тест-страж: в исходниках пакета нет личных абсолютных путей

## 6. Документация и проверка

- [x] 6.1 README: раздел о политиках безопасности и их границах
- [x] 6.2 `docs/GUIDE.md`: секция `security`, команды, примеры правил
- [x] 6.3 `docs/PARITY.md`: отметить закрытие гэпа безопасности
- [x] 6.4 CHANGELOG: запись о добавлении
- [x] 6.5 Прогон `pytest src/tests -q` (все зелёные) и `openspec validate --all`
