## 1. Хранилище снимков

- [x] 1.1 Создать пакет `src/prokop/checkpoints/` (`__init__.py`, `store.py`, `tool.py`)
- [x] 1.2 `store.py`: модель записи снимка и манифеста, атомарная запись индекса
- [x] 1.3 `store.py`: обход путей с правилами игнорирования и потолком размера файла
- [x] 1.4 `store.py`: `create()` — снимок набора путей, отчёт о пропущенных, уникальный идентификатор
- [x] 1.5 `store.py`: `list()` и `get()` — список снимков от новых к старым
- [x] 1.6 `store.py`: `restore()` — возврат содержимого, воссоздание отсутствующих, страховочный снимок, флаг удаления лишних
- [x] 1.7 `store.py`: `prune()` — удаление старых снимков с сохранением N последних, идемпотентно
- [x] 1.8 `store.py`: отчёт о восстановлении с перечнем путей и идентификатором страховочного снимка
- [x] 1.9 `config.py`: секция `checkpoints` (включённость, потолок записей, потолок размера, дополнительные шаблоны игнорирования)
- [x] 1.10 `store.py`: разбор секции конфигурации в настройки хранилища

## 2. Инструмент агента

- [x] 2.1 `tool.py`: инструмент `checkpoint` с действиями `create`, `list`, `restore`, `prune`
- [x] 2.2 `tool.py`: результат в формате ядра (JSON-строка), ошибки — без падения
- [x] 2.3 `tools/toolsets.py`: набор `checkpoints`
- [x] 2.4 Неизвестное действие возвращает ошибку и не меняет состояние

## 3. CLI

- [x] 3.1 `prokop checkpoints list`
- [x] 3.2 `prokop checkpoints create <путь>... [--label L]`
- [x] 3.3 `prokop checkpoints restore <id> [--remove-extra]`
- [x] 3.4 `prokop checkpoints prune [--keep N]`
- [x] 3.5 Ошибки: неизвестный снимок и несуществующий путь — код `1`

## 4. Тесты

- [x] 4.1 Снимок каталога: содержимое, манифест, число файлов
- [x] 4.2 Снимок отдельного файла; кириллица и пробелы в пути
- [x] 4.3 Уникальность идентификаторов при создании подряд
- [x] 4.4 Несуществующий путь пропускается, снимок создаётся
- [x] 4.5 Восстановление изменённого и удалённого файла
- [x] 4.6 Лишние файлы сохраняются по умолчанию и удаляются по флагу
- [x] 4.7 Страховочный снимок создаётся; откат отката работает
- [x] 4.8 Игнорирование служебных каталогов; дополнительные шаблоны из конфигурации
- [x] 4.9 Потолок размера файла: файл пропускается и отмечен в отчёте
- [x] 4.10 Потолок числа снимков: старые удаляются; `prune` идемпотентен
- [x] 4.11 Неизвестный снимок — ошибка, файлы не изменяются
- [x] 4.12 Инструмент: create/list/restore/prune через диспетчер ядра; неизвестное действие
- [x] 4.13 CLI `checkpoints list|create|restore|prune` без ключа модели
- [x] 4.14 Тест-страж: в исходниках пакета нет личных абсолютных путей

## 5. Документация и проверка

- [x] 5.1 README: раздел о снимках и откате
- [x] 5.2 `docs/GUIDE.md`: команды, конфигурация, безопасность отката
- [x] 5.3 `docs/PARITY.md`: отметить закрытие гэпа checkpoints
- [x] 5.4 CHANGELOG: запись о добавлении
- [x] 5.5 Прогон `pytest src/tests -q` (все зелёные) и `openspec validate --all`
